# Dependencies
node_modules/
app/node_modules/

# Next.js build / static export
app/.next/
app/out/

# Tauri / Rust build artifacts
app/src-tauri/target/

# Corpus (markdown round-trip fixtures, generated/local)
app/corpus/

# TypeScript build info
*.tsbuildinfo

# Editor / OS
.DS_Store

# Env
.env*

# Screenshots scratch
app/.shots/

# continuum ephemeral local working store (DEC-008/010: drafts / status /
# annotations / worktree refs — never committed; durable docs land via PR)
.continuum/
